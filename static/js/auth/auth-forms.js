/**
 * Auth Form Helpers
 * -----------------
 * Central place for login/register UX:
 * - Client-side validation + inline errors for sign up / login
 * - Password visibility toggles + strength meter hooks
 * - Handle OAuth button states + disable duplicate submissions
 * Each auth template should call AuthForms.init() once the DOM is ready.
 */
window.AuthForms = window.AuthForms || {
  init() {
    // TODO: wire up validation + submit interceptors for auth templates.
  }
};
