/**
 * Notes Trash / Recently Deleted
 * ------------------------------
 * Handles restoring or permanently deleting notes in the trash view:
 * - Bulk select + restore/delete actions
 * - Countdown for auto-purge + timers per note
 * - Shares folder count updates with FolderPanel
 * Keep the API surface small via NotesTrash.init().
 */
window.NotesTrash = window.NotesTrash || {
  init() {
    // TODO: wire up restore/delete buttons on /notes/recently-deleted.
  }
};
