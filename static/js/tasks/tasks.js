/**
 * Tasks Board Interactions
 * ------------------------
 * Owns the kanban-style lists UI on /tasks:
 * - Handle drag/drop ordering + persists via /lists/reorder
 * - Inline task creation + completion toggles per list
 * - Empty state prompts + list auto-scroll logic
 * Keep per-feature helpers here; expose TasksBoard.init() for the template.
 */
window.TasksBoard = window.TasksBoard || {
  init() {
    // TODO: boot drag/drop + list listeners when /tasks renders.
  }
};
